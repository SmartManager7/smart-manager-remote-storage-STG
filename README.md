# Smart manager remote storage STG

## Local development

Before running `npm install` you have to do a `npm link @smart-manager/tools`, as the tools is not published to npm and required in this project.
